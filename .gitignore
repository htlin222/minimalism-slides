# Re-include HTML — global gitignore excludes *.html by default
!*.html

.DS_Store

# Build artifacts
*.pdf
dist/

# Wrangler / Cloudflare cache
.wrangler/

# Deploy configs hold a personal domain — track only the .example.* templates.
slides.json
worker.toml
